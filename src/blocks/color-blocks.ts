/**
 * Color Blocks
 *
 * REMOVED: All color blocks (ColorLFO, HSVToColor, HsvToRgb, ApplyOpacity) have been
 * removed as part of the block library reduction for the type system migration.
 * These blocks had complex kernel dependencies that created migration friction.
 * They will be rewritten with improved kernels after the type system migration is complete.
 *
 * NOTE: ConstColor was previously removed - use the unified polymorphic Const block instead.
 */
