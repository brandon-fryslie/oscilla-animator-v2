/**
 * WASM Renderer Canonical Boundary Contract
 *
 * [LAW:one-source-of-truth] All JSON payloads that cross JS→Rust/WASM renderer
 * boundaries are defined, validated, and normalized here.
 *
 * STUB: V1 types removed (scorched earth). New spec-aligned types
 * (PipelineInstallPayload, ExprIR, StatementIR, InstallReceipt) will be
 * added in Phase 2 of the single-triangle vertical slice.
 */

// Phase 2 will add: PipelineInstallPayload, MemoryManifest, ExecutionRoster,
// ExprIR, StatementIR, InstallReceipt, CompilationDiagnostic, and validation.
