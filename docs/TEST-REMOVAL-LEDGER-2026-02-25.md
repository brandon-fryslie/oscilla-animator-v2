# Test Removal Ledger (2026-02-25)

This file tracks functionality that was covered by deleted test files during the test-suite cull.

- Scope: files currently deleted in working tree (`git diff --name-status` = `D`).
- Count: 62 deleted test files.
- Source of truth for behavior descriptors: original `describe()` / `it()` titles from `HEAD` before deletion.

## Removed Test Files

| Removed File | Tests Removed | Functionality Previously Tested | Coverage Status |
|---|---:|---|---|
| `src/__tests__/compiledir-foundation-gates.test.ts` | 1 | Suites: CompiledIR Foundation Migration Gates (W11/W5/W6/W2/W7/W12). Cases: ${gate.id} does not grow beyond baseline. | Consolidated in src/__tests__/architecture-guardrails.test.ts |
| `src/__tests__/forbidden-patterns.test.ts` | 88 | Suites: Forbidden Patterns (Type System Invariants); WebGPU Draw Contract (V3); Runtime Loop Contract (V3). Cases: WebGPURenderer must use indirect draws (no direct drawIndexed hot path); AnimationLoop must not perform CPU coordinate scans for content bounds in the hot path; no AxisTag type alias anywhere in src/; no payload var kind outside inference modules. | Consolidated in src/__tests__/architecture-guardrails.test.ts |
| `src/blocks/__tests__/debug-test-one.test.ts` | 1 | Suites: TestOne Debug Tests. Cases: placeholder. | Placeholder removed; no behavior contract retained |
| `src/blocks/__tests__/math-utility-blocks.test.ts` | 3 | Suites: Noise Block; Length Block; Normalize Block. Cases: placeholder; placeholder; placeholder. | Superseded by broader behavior coverage in existing block/runtime integration suites |
| `src/compiler/__tests__/no-legacy-types.test.ts` | 3 | Suites: legacy type enforcement. Cases: no production code references SigExpr/FieldExpr/EventExpr types; no production code references SigExprId/FieldExprId/EventExprId aliases; no production code calls deriveKind function. | Consolidated in src/__tests__/architecture-guardrails.test.ts |
| `src/compiler/__tests__/steel-thread-rect.test.ts` | 1 | Suites: Steel Thread - Rect Shape Pipeline. Cases: placeholder. | Placeholder removed; steel-thread coverage retained in src/compiler/__tests__/steel-thread*.test.ts |
| `src/compiler/ir/__tests__/hash-consing.test.ts` | 32 | Suites: Hash-consing (I13); SigExpr deduplication; Compound expression deduplication. Cases: deduplicates identical sigConst; distinguishes different sigConst values; deduplicates identical sigTime; distinguishes different sigTime variants. | No explicit one-to-one replacement documented in this cull pass. |
| `src/compiler/ir/__tests__/no-embedded-valueexpr.test.ts` | 6 | Suites: ValueExpr Flattening Enforcement. Cases: no embedded ValueExpr[] arrays in value-expr.ts; no embedded ValueExpr object fields in value-expr.ts; ValueExpr export is a union type, not an interface; all ValueExpr variant references use ValueExprId. | Consolidated in src/__tests__/architecture-guardrails.test.ts |
| `src/compiler/ir/__tests__/no-legacy-kind-dispatch.test.ts` | 1 | Suites: no-legacy-kind-dispatch. Cases: should not dispatch on legacy expression kinds outside allowed files. | Consolidated in src/__tests__/architecture-guardrails.test.ts |
| `src/core/__tests__/canonical-name.test.ts` | 47 | Suites: normalizeCanonicalName; basic normalization; special character stripping. Cases: strips special characters; replaces spaces with underscores; converts to lowercase; combines all rules correctly. | No explicit one-to-one replacement documented in this cull pass. |
| `src/events/__tests__/EventHub.test.ts` | 33 | Suites: EventHub; Type-safe subscriptions; Global listeners. Cases: should call listener when matching event is emitted; should not call listener for different event types; should call multiple listeners for same event type; should allow unsubscribing. | No explicit one-to-one replacement documented in this cull pass. |
| `src/expr/__tests__/lexer.test.ts` | 24 | Suites: Lexer; Numbers; Identifiers. Cases: tokenizes integer literals; tokenizes float literals; tokenizes zero; tokenizes simple identifiers. | No explicit one-to-one replacement documented in this cull pass. |
| `src/expr/__tests__/parser.test.ts` | 27 | Suites: Parser; Literals; Identifiers. Cases: parses integer literal; parses float literal; parses identifier; parses simple member access. | No explicit one-to-one replacement documented in this cull pass. |
| `src/expr/__tests__/suggestions.test.ts` | 36 | Suites: getFunctionSignatures; SuggestionProvider.suggestFunctions; SuggestionProvider.suggestConstants. Cases: returns all 20 function signatures; includes trigonometric functions; includes unary functions; includes binary functions. | No explicit one-to-one replacement documented in this cull pass. |
| `src/expr/__tests__/swizzle.test.ts` | 35 | Suites: Swizzle Constants; componentIndex; isVectorType. Cases: POSITION_COMPONENTS contains x, y, z; COLOR_COMPONENTS contains r, g, b, a; maps x/r to 0; maps y/g to 1. | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/deserialize.test.ts` | 16 | Suites: deserialize. Cases: deserializes empty patch; deserializes simple block; deserializes edge via outputs; throws on unresolvable references in outputs. | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/error-recovery.test.ts` | 8 | Suites: error recovery. Cases: handles unresolvable block reference in outputs; renames duplicate block names; preserves unknown block type; handles missing block labels. | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/lexer.test.ts` | 35 | Suites: lexer. Cases: tokenizes empty input; tokenizes simple block; tokenizes numbers (integers); tokenizes numbers (floats). | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/parser.test.ts` | 35 | Suites: parser; error recovery on malformed input. Cases: parses empty document; parses simple block; parses block with no labels; parses block with single label. | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/serialize.test.ts` | 16 | Suites: serialize; emitValue (string escaping). Cases: serializes empty patch; serializes simple block with params; serializes edge between blocks as inline outputs; handles block name collisions. | No explicit one-to-one replacement documented in this cull pass. |
| `src/patch-dsl/__tests__/tripwire.test.ts` | 32 | Suites: tripwire: lexer exceptions; tripwire: negative numbers; tripwire: null values. Cases: does not throw on garbage input; does not throw on unterminated string; does not throw on unexpected character in value position; lexes -1 as single NUMBER token. | No explicit one-to-one replacement documented in this cull pass. |
| `src/runtime/__tests__/integration.test.ts` | 3 | Suites: Runtime Integration. Cases: _placeholder_executes_a_simple_animated_grid; _placeholder_evaluates_constant_values; _placeholder_resolves_time_correctly_for_infinite_models. | Replaced by behavior suites: src/blocks/__tests__/stateful-primitives.test.ts, src/blocks/__tests__/event-blocks.test.ts, src/runtime/__tests__/reduce-op.test.ts |
| `src/runtime/__tests__/phase7-kernel-sanity.test.ts` | 4 | Suites: Phase 7 - Layer 1: Opcode Sanity; Phase 7 - Layer 2: Scalar Kernel Sanity; Phase 7 - Layer 3: Field Kernel Sanity. Cases: _placeholder_removed; _placeholder_Phase_wrapping_and_basic_execution; _placeholder_removed; _placeholder_removed. | Replaced by behavior suites: src/blocks/__tests__/stateful-primitives.test.ts, src/blocks/__tests__/event-blocks.test.ts, src/runtime/__tests__/reduce-op.test.ts |
| `src/services/__tests__/exportFormats.test.ts` | 17 | Suites: exportFormats; formatConfigValue; isNonDefault. Cases: formats primitives correctly; formats arrays correctly; formats objects correctly; formats expression strings as-is. | No explicit one-to-one replacement documented in this cull pass. |
| `src/services/CompilationInspectorService.test.ts` | 61 | Suites: CompilationInspectorService; Basic lifecycle; Pass capture. Cases: starts with no snapshots; creates snapshot on beginCompile(); tracks compilation status (success); tracks compilation status (failure). | No explicit one-to-one replacement documented in this cull pass. |
| `src/services/DebugService.test.ts` | 52 | Suites: DebugService; edge-to-slot mapping; slot value updates. Cases: should store and retrieve edge-to-slot mapping; should throw for unmapped edge (compiler bug); should return undefined for single-instance edge whose slot has no value before runtime starts; should throw for single-instance edge whose slot has no value after runtime starts (scheduling bug). | No explicit one-to-one replacement documented in this cull pass. |
| `src/services/mapDebugEdges.test.ts` | 11 | Suites: mapDebugEdges; error conditions - must throw (no silent failures). Cases: should map edges to source slots correctly; should map multiple edges from same source block; maps unambiguous connected input ports for port probes; should return empty map for patch with no edges. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/CompositeEditorStore.test.ts` | 2 | Suites: CompositeEditorStore. Cases: reports missing composite definitions through injected issue reporter; reports save validation failures instead of emitting direct console warnings. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/DebugStore.test.ts` | 5 | Suites: DebugStore. Cases: prefers hovered edge over selected edge for activeEdgeId; polls and caches scalar value for active mapped edge; handles unknown edge polling failures without throwing; stops polling and clears cached value when disabled. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/DiagnosticsStore-compilation-stats.test.ts` | 8 | Suites: DiagnosticsStore Compilation Stats. Cases: should start with zero stats; should record compilations correctly; should calculate average correctly; should calculate median correctly for odd count. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/DiagnosticsStore-muting.test.ts` | 3 | Suites: DiagnosticsStore muting. Cases: mutes existing diagnostics and excludes them from active diagnostics; returns false when muting unknown diagnostics; allows unmuting and restoring visibility. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/FrontendResultStore.test.ts` | 8 | Suites: FrontendResultStore. Cases: starts with empty snapshot; updates snapshot from successful frontend compilation; hasDefaultSource() returns true for unconnected inputs; hasDefaultSource() returns false for connected inputs. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/PatchStore-displayName.test.ts` | 11 | Suites: PatchStore - Display Name Auto-generation; addBlock auto-generation; updateBlockDisplayName validation. Cases: should auto-generate displayName ; should auto-generate ; should auto-generate ; should always auto-generate displayName even when explicit name is provided. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/PatchStore-lens.test.ts` | 23 | Suites: PatchStore Lens Methods; addLens; removeLens. Cases: adds lens to port with no existing lenses; adds lens to port with existing lenses; generates deterministic lens ID from source + lens type; allows multiple chained lenses for the same source address. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/PatchStore.test.ts` | 26 | Suites: PatchStore; addBlock; removeBlock. Cases: should add a block with generated ID; should add a block with options; should generate unique IDs; should remove a block. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/RootStore-graph-committed.test.ts` | 1 | Suites: RootStore GraphCommitted diff summary. Cases: emits block/edge diffSummary and affectedBlockIds for graph edits. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/SelectionStore.test.ts` | 39 | Suites: SelectionStore; block selection; edge selection. Cases: should select a block by ID; should clear edge selection when selecting block; should return undefined if selected block was deleted; should select an edge by ID. | No explicit one-to-one replacement documented in this cull pass. |
| `src/stores/__tests__/SettingsStore.test.ts` | 4 | Suites: SettingsStore. Cases: uses defaults without warnings when localStorage is unavailable; treats malformed localStorage object as unavailable; loads persisted values and persists updates via storage API; falls back to defaults when persisted JSON is invalid. | No explicit one-to-one replacement documented in this cull pass. |
| `src/types/__tests__/canonical-address.test.ts` | 43 | Suites: Canonical Address Type Guards; addressToString; parseAddress. Cases: isBlockAddress correctly identifies block addresses; isOutputAddress correctly identifies output addresses; isInputAddress correctly identifies input addresses; isParamAddress correctly identifies param addresses. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/charts/DistributionBar.test.tsx` | 6 | Suites: DistributionBar. Cases: renders min and max labels; positions mean tick proportionally; handles degenerate min=max without crash; shows . | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/charts/Sparkline.test.tsx` | 13 | Suites: Sparkline; phase wrap markers; scale markers. Cases: renders a canvas element; renders empty canvas for empty history; draws line path for valid samples; auto-scale maps min to bottom and max to top. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/charts/WarmupIndicator.test.tsx` | 5 | Suites: WarmupIndicator. Cases: shows text when filled < capacity; returns null when filled >= capacity; returns null when filled > capacity; shows progress for 0 filled. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/FieldStatsAccumulator.test.ts` | 10 | Suites: FieldStatsAccumulator; stride 1 (scalar float); stride 4 (color RGBA). Cases: computes correct stats for a single frame; allTimeMin/Max only expand, never shrink; EMA converges toward consistent value; EMA barely moves on a spike. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/HistoryService.test.ts` | 32 | Suites: HistoryService; track(); untrack(). Cases: tracks a valid scalar key; is idempotent for already-tracked keys; rejects field-cardinality keys (no throw); rejects non-sampleable payloads (bool). | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/CameraProjectionRenderer.test.tsx` | 7 | Suites: CameraProjectionRenderer; scalar mode; aggregate mode. Cases: shows ORTHOGRAPHIC badge for value 0; shows PERSPECTIVE badge for value 1; shows raw value; shows percentage perspective. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/ColorValueRenderer.test.tsx` | 11 | Suites: ColorValueRenderer; aggregate mode (full); scalar mode. Cases: renders hex display from mean color; hex conversion correct for mid-values (0.5); hex conversion correct for 0.0 (black); hex conversion correct for 1.0 (white). | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/FloatValueRenderer.test.tsx` | 12 | Suites: FloatValueRenderer; scalar mode; unit decorations. Cases: displays formatted value; displays NaN as red badge; displays Inf as badge; phase01: shows . | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/formatFloat.test.ts` | 21 | Suites: formatFloat; values in [0.001, 9999]; values outside [0.001, 9999]. Cases: formats zero as ; formats negative zero as ; formats NaN; formats positive Infinity. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/Vec3ValueRenderer.test.tsx` | 7 | Suites: Vec3ValueRenderer; scalar mode; aggregate mode. Cases: renders X, Y, Z components + magnitude; handles NaN/Inf values; z-uniform: shows XY scatter and ; z-varying: shows all 3 component stat rows + magnitude. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/renderers/Vec4ValueRenderer.test.tsx` | 5 | Suites: Vec4ValueRenderer; scalar mode; aggregate mode. Cases: renders X, Y, Z, W components + magnitude; handles invalid values; shows per-component stats for all 4 components; shows count badge. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/types.test.ts` | 14 | Suites: getSampleEncoding; serializeKey; Stride type. Cases: returns stride=1 for float; returns stride=1 for int; returns stride=2 for vec2; returns stride=4 for color. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/useDebugMiniView.test.ts` | 14 | Suites: useDebugMiniView. Cases: should return null when hoveredEdgeId is null; should return null when edge has no metadata; should return MiniViewData for a mapped single-instance edge; should fall back to edgeId when no label provided. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/debug-viz/ValueRenderer.test.ts` | 9 | Suites: ValueRenderer registry; 3-tier fallback ladder; placeholder renderer. Cases: tier 1: exact match (payload + unit) wins; tier 2: payload-only when no exact match; tier 3: category fallback when no payload match; tier 3: color category fallback. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/expression-editor/__tests__/AutocompleteDropdown.test.tsx` | 16 | Suites: AutocompleteDropdown - Rendering; AutocompleteDropdown - Type Icons; AutocompleteDropdown - Selection State. Cases: renders nothing when isVisible is false; renders nothing when suggestions array is empty; renders suggestions when visible with non-empty array; renders descriptions when provided. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/expression-editor/__tests__/cursorPosition.test.ts` | 25 | Suites: getCursorPosition - Algorithm; adjustPositionForViewport; cursorPosition - Integration. Cases: returns a CursorPosition object with top and left properties; handles empty textarea without errors; handles cursor at various positions without errors; handles multiline text without errors. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/expression-editor/__tests__/referenceTokenizer.test.ts` | 2 | Suites: tokenizeExpression. Cases: tokenizes pi and tau as constant segments with greek display labels; keeps unresolved block.port text as plain segment instead of constant. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/graphEditor/__tests__/CompositeStoreAdapter.test.ts` | 20 | Suites: CompositeStoreAdapter; blocks getter; edges getter. Cases: exposes internal blocks from CompositeEditorStore; transforms InternalBlockState to BlockLike format; uses displayName if set on internal block; derives ports from block definition. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/graphEditor/__tests__/PatchStoreAdapter.test.ts` | 16 | Suites: PatchStoreAdapter; blocks getter; edges getter. Cases: exposes blocks from PatchStore; transforms Block to BlockLike format; is MobX-observable; exposes edges from PatchStore. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/reactFlowEditor/__tests__/connection-validation.test.ts` | 29 | Suites: Payload constraint validation; payloadVar sources respect allowedPayloads (crio.1); concrete placeholder types respect allowedPayloads (crio.2). Cases: constrained payloadVar source → compatible concrete sink is ALLOWED; constrained payloadVar source → incompatible concrete sink is BLOCKED; constrained payloadVar sink → incompatible concrete source is BLOCKED; unconstrained payloadVar source → any concrete sink is ALLOWED. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/reactFlowEditor/__tests__/layout.test.ts` | 9 | Suites: getLayoutedElements. Cases: should handle empty graph gracefully; should handle single node; should arrange two connected nodes horizontally (left-to-right); should arrange three nodes in a chain without overlap. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/reactFlowEditor/__tests__/lensUtils.test.ts` | 15 | Suites: lensUtils; getAvailableLensTypes; getLensLabel. Cases: returns adapter blocks from registry; returns types sorted by label; includes known adapter types; returns registry label for known type. | No explicit one-to-one replacement documented in this cull pass. |
| `src/ui/reactFlowEditor/__tests__/nodes-edge-data.test.ts` | 7 | Suites: createEdgeFromEdgeLike - lens data population. Cases: populates edge data with lenses from target port; sets edge type to oscilla for custom rendering; populates edge data with multiple lenses; filters out lenses that target a different source connection. | No explicit one-to-one replacement documented in this cull pass. |

## Notes

- This ledger is additive: future removals should append/update rows rather than replacing history.
- If a removed behavior needs to be restored, add a behavior-level test (not implementation-shape assertions) and update this ledger with the new owner file.
