@@ -168,7 +184,11 @@
   });
 
   it('allocates slots for pure blocks', () => {
-    const type = canonicalType(FLOAT, { kind: 'none' }, {
+    const type = canonicalType(
+      FLOAT,
+      { kind: 'none' },
+      {
+        binding: { kind: 'inst', value: 'free' },
         cardinality: cardinalityOne(),
         temporality: temporalityContinuous(),
-        perspective: { kind: 'inst', id: 'default' as any },
+        perspective: { kind: 'inst', value: { kind: 'default' } },
